# ashiq has been renamed to schemagate

```bash
pip install schemagate
```

`import ashiq` keeps working: it re-exports `schemagate` and emits a
`DeprecationWarning`. Everything else, including the docs, lives at
https://github.com/ashishsinha1602/schemagate
